#pragma once
/**
 * PC shadow for dolphin/types.h
 * Just re-exports port/types.h so all other dolphin/ headers
 * get the fixed-width integers rather than the GC originals.
 */
#include "port/types.h"

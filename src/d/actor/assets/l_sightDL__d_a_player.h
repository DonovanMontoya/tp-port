#pragma once

// PC fallback display list blob for daPy_sightPacket_c::draw().
// The original generated asset header is not present in this workspace;
// a zeroed placeholder keeps the host build moving until the real asset
// extraction path is wired in.
static const unsigned char l_sightDL[0x80] = {};
